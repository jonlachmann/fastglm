#PKG_CXXFLAGS = -DNDEBUG -O3 -mavx -march=native
PKG_CXXFLAGS = -O0 -fsanitize=address
#PKG_LIBS = `$(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()"` $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) -lasan -lubsan

PKG_LDFLAGS=-fsanitize=address

CXX_STD = CXX11
